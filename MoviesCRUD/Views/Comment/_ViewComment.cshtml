﻿@using MoviesCRUD.Models.ViewModel
@model CommentsVM

<style>
    body {
        background: #eee
    }

    .date {
        font-size: 11px
    }

    .comment-text {
        font-size: 12px
    }

    .fs-12 {
        font-size: 12px
    }

    .shadow-none {
        box-shadow: none
    }

    .name {
        color: #007bff
    }

    .cursor:hover {
        color: blue
    }

    .cursor {
        cursor: pointer
    }

    .textarea {
        resize: none
    }
</style>
<div class="col-md-12">
    @if (Model.Comments != null)
    {
        @foreach (var comment in @Model.Comments)
        {
            <div class="container mt-5">
                <div class="d-flex justify-content-center row">
                    <div class="col-md-8">
                        <div class="d-flex flex-column comment-section">
                            <div class="bg-white p-2">
                                <div class="d-flex flex-row user-info">
                                    <input type="hidden" id="CommentId" value="@comment.CommentId">
                                    <img class="rounded-circle" src="https://i.imgur.com/RpzrMR2.jpg" width="40">
                                    <div class="d-flex flex-column justify-content-start ml-2"><span class="d-block font-weight-bold name">@comment.UserName</span><span class="date text-black-50">@comment.CreatedAt</span></div>
                                </div>
                                <div class="mt-2">
                                    <p class="comment-text">@comment.Text</p>
                                </div>
                            </div>
                            <div class="bg-white">
                                <div class="d-flex flex-row fs-12">
                                    <div class="like p-2 cursor"><i class="fa fa-thumbs-o-up"></i><span class="ml-1">Like</span></div>
                                    <div class="like p-2 cursor"><i class="fa fa-commenting-o"></i><span class="ml-1">Comment</span></div>
                                    <div class="like p-2 cursor"><i class="fa fa-share"></i><span class="ml-1">Share</span></div>
                                </div>
                            </div>
                            @if (User.Identity.Name == comment.UserName)
                            {
                                <div>
                                    <button class="btn btn-danger" style="border-radius:20px;" onclick="DeleteComment()">Delete</button>
                                </div>
                            }

                        </div>
                    </div>
                </div>
            </div>

        }
    }
</div>

@*<div class="comment-field d-flex justify-content-between align-items-center">
    <div>
        <input type="hidden" id="CommentId" value="@comment.CommentId">
        <strong class="text-danger">@comment.UserName</strong><sub>@comment.CreatedAt</sub>
        <p>@comment.Text</p>
    </div>
    @if (User.Identity.Name == comment.UserName)
    {
        <div>
            <button class="btn btn-danger" style="border-radius:20px;" onclick="DeleteComment()">Delete</button>
        </div>
    }
</div>*@
